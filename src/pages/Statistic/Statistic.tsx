import { FC } from 'react';
import ContentWrapper from '../../layouts/ContentWrapper';

const Statistic: FC = () => {
  return <ContentWrapper>Statistic</ContentWrapper>;
};

export default Statistic;
