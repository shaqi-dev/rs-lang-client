import { FC } from 'react';
import ContentWrapper from '../../layouts/ContentWrapper';

const Statistics: FC = () => {
  return <ContentWrapper>Statistics</ContentWrapper>;
};

export default Statistics;
