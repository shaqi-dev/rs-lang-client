import Counter from './Counter';
import { counterSlice, increment, decrement, incrementByAmount, selectCount } from './CounterSlice';

export default Counter;

export { counterSlice, increment, decrement, incrementByAmount, selectCount };
